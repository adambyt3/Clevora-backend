<div class="row justify-content-center">
  <div class="col-lg-8 col-xl-7">
    <div class="card-premium p-4">
      <div class="mb-4">
        <a href="/siswa" class="btn btn-premium btn-sm p-1 px-3 border border-2 text-decoration-none mb-3 d-inline-flex align-items-center gap-1" style="font-size: 0.85rem; color: var(--purple);">
          <i class="fa-solid fa-arrow-left"></i> Kembali ke Daftar
        </a>
        <h4 class="fw-bold mb-1" style="color: var(--purple-dark);">Tambah Siswa Baru</h4>
        <p class="text-muted mb-0 small">Masukkan data diri siswa lengkap. Password wajib diisi untuk pembuatan akun awal.</p>
      </div>

      <form action="/siswa/tambah" method="POST" class="form-premium needs-validation" novalidate>
        <div class="row g-3">
          <!-- Nama -->
          <div class="col-12">
            <label for="nama" class="form-label fw-bold">Nama Lengkap Siswa</label>
            <input type="text" class="form-control" id="nama" name="nama" placeholder="Contoh: Budi Santoso" required>
            <div class="invalid-feedback">Nama lengkap wajib diisi.</div>
          </div>

          <!-- Email -->
          <div class="col-md-6">
            <label for="email" class="form-label fw-bold">Alamat Email</label>
            <input type="email" class="form-control" id="email" name="email" placeholder="budi@clevora.id" required>
            <div class="invalid-feedback">Email valid wajib diisi.</div>
          </div>

          <!-- Password -->
          <div class="col-md-6">
            <label for="password" class="form-label fw-bold">Kata Sandi Akun</label>
            <input type="password" class="form-control" id="password" name="password" placeholder="Min. 6 karakter" required minlength="6">
            <div class="invalid-feedback">Kata sandi minimal 6 karakter.</div>
          </div>

          <!-- NISN -->
          <div class="col-md-6">
            <label for="nisn" class="form-label fw-bold">NISN (Nomor Induk Siswa Nasional)</label>
            <input type="text" class="form-control" id="nisn" name="nisn" placeholder="10 digit nomor NISN" required pattern="\d{10}">
            <div class="invalid-feedback">NISN wajib berupa 10 digit angka.</div>
          </div>

          <!-- Kelas -->
          <div class="col-md-6">
            <label for="kelas" class="form-label fw-bold">Pilih Kelas</label>
            <select class="form-select" id="kelas" name="kelas" required>
              <option value="" disabled selected>Pilih salah satu...</option>
              <option value="6A">Kelas 6A</option>
              <option value="6B">Kelas 6B</option>
              <option value="6C">Kelas 6C</option>
              <option value="6D">Kelas 6D</option>
            </select>
            <div class="invalid-feedback">Silakan pilih kelas siswa.</div>
          </div>

          <!-- Sekolah -->
          <div class="col-12">
            <label for="sekolah" class="form-label fw-bold">Nama Sekolah asal</label>
            <input type="text" class="form-control" id="sekolah" name="sekolah" placeholder="SD Negeri Clevora Jaya" required>
            <div class="invalid-feedback">Nama sekolah wajib diisi.</div>
          </div>
        </div>

        <div class="d-flex justify-content-end gap-2 mt-4">
          <a href="/siswa" class="btn btn-premium btn-light border border-2">Batal</a>
          <button type="submit" class="btn btn-premium btn-premium-primary">
            <i class="fa-solid fa-floppy-disk me-1"></i> Simpan Siswa
          </button>
        </div>
      </form>
    </div>
  </div>
</div>

<script>
  // Bootstrap 5 client-side validation
  (function () {
    'use strict'
    var forms = document.querySelectorAll('.needs-validation')
    Array.prototype.slice.call(forms)
      .forEach(function (form) {
        form.addEventListener('submit', function (event) {
          if (!form.checkValidity()) {
            event.preventDefault()
            event.stopPropagation()
          }
          form.classList.add('was-validated')
        }, false)
      })
  })()
</script>
